apiVersion: v1
kind: Service
metadata:
  name: {{ include "es-usage.fullname" . }}
  labels:
    {{- include "es-usage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "es-usage.selectorLabels" . | nindent 4 }}
